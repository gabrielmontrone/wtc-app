<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Cleartext (HTTP) traffic is only permitted to the local development backend:
      10.0.2.2 is the host machine as seen from the Android emulator. Everything else
      must use TLS. Tighten/remove this block before shipping to production.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
